# Charter: Confirmation R1 — D2 Repair 1

Status: **prepared, inactive — no seat assigned or dispatched**

This fresh confirmation review is contingent on a completed Repair 1 artifact.
It becomes executable only if the owner later approves this exact charter and
explicitly releases the reviewer seat under ADR-0034.

- **Proposed role:** confirmation reviewer, Medium tier, fresh independent
  context.
- **Inputs after release:** `charter-repair1.md`, the Repair 1 design and
  examination, Round 1 reviews, `round-1-triage.md`, named ratified ADRs, and
  the same committed runner/package/explanation surfaces measured in R1.
- **Exclusions:** no production implementation, no ADR draft, no evaluation
  analysis, and no other in-progress reviewer output.

## Measurements

Report each check as pass, fail, or not run, with a concrete source or paper
case:

1. Exactly one successor line-16 producer exists; no package or traversal
   behavior is misdescribed as dynamic policy selection.
2. Qualified-zero reaches the unchanged ordinary result without either new
   declaration, and the reduction algebra plus lazy-expression claim are
   supported by the committed evaluator.
3. Qualified-positive requires both declarations; evaluated accesses, not a
   static pins list or `requires` alone, provide the declaration-to-line-16
   displacement path.
4. Present `yes` produces an honest, walkable disposition: either currently
   supported or explicitly a versioned production condition. No false custom
   blocked-code claim remains.
5. The no-declaration/signal mutual exclusion still rejects declaration-first,
   signal-first, and same-batch attempts before mutation, and no worksheet
   input reaches box 2a, its signal, or recorded-non-composable content.
6. Every unimplemented surface is labelled a production condition and carries
   a proposed authoritative-surface kill test; no test or rule is written.

The review ends with a narrow `confirmed` / `not confirmed` verdict on whether
Repair 1 resolves the R1 decision-blocking findings. It does not reopen D1,
D3, statutory-rate fidelity, or presentation.
