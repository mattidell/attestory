# Charter: Iteration 1 — Conditional Multi-Dependency Non-Publication

Status: **complete — external incumbent build landed**

Date: 2026-07-18. This is the first builder charter under the approved
Conditional Multi-Dependency Non-Publication plan. The owner released this
exact charter while retaining no internal dispatch; the external incumbent
build landed with its two required documents. This charter is complete.

- **Proposed role:** incumbent builder, High tier, independent context.
- **Proposed working location:** docs/prototypes/conditional-multi-dependency-nonpublication/it1/.
- **Evidence boundary:** Rung 1 paper only. Do not run a Rung-2 probe unless
  the owner separately releases the seat and the paper result meets the plan’s
  exact escalation condition.
- **Output boundary:** two documents only; no production code, schemas, tests,
  ADR, evaluation analysis, process-log edit, git write, or role dispatch.

## Binding context

Read the approved topic plan and decision inventory; the D2 confirmation
triage and D2 plan only for the consumer boundary; the governance set; and
ADRs 0006, 0010, 0013, 0024, 0025, 0030, 0034, and 0036. Inspect committed
evaluator, runner, projection, explanation, record-schema, NPE-schema, and
rule-artifact schema source only as needed to cite a paper construction.

Do not reopen D2 worksheet arithmetic, declaration domain or meaning,
present-yes handling, admission interlock, package ownership, Schedule B,
tax-law fidelity, or production implementation. All examples are synthetic
demo labels only.

## Assignment

Design one generic declared-artifact shape for CMDN-P1 through CMDN-P3:

1. A declared condition activates a declared finite dependency set.
2. When active, one non-publication disposition and its explanation name all
   and only currently absent factual members.
3. When inactive, those members are not demanded, named missing, or pinned.
4. A successful active publication pins the relevant findings through existing
   declared input/choice edges; later supersession follows ordinary currency.
5. The shape places no tax meaning, dependency enumeration, or explanation
   repair in runner/UI code, and introduces no third standing-affecting edge.

State precisely which aspects are existing committed capability, proposed
versioned contract, or production condition. Do not claim an unbuilt surface
is available at HEAD.

## Required paper cases

Complete all six approved cases:

1. Inactive positive: all members absent, unaffected result publishes.
2. Active positive: all members present, result publishes and pins them.
3. Active multi-absence: two members absent, one walk names both.
4. Active partial absence: one present, one absent, walk names only one.
5. Lifecycle: inactive → active/both absent → one contribution/re-run → one
   absent → second contribution/re-run → publish → member supersession.
6. No reach-around: demonstrate why a tax-specific runner, UI, or hidden
   post-processing list cannot supply missing-member semantics.

Supply a producer → authority → consumer → failure map for each CMDN
proposition. If paper cannot distinguish a declared shape from hidden runner
policy, state the exact unresolved boundary and stop; do not invent a probe or
implementation.

## Outputs and stop

Write only:

- it1/design.md
- examination-it1.md, separately reporting CMDN-P1, CMDN-P2, and CMDN-P3 as
  settled-at-rung or unresolved.

Before writing, echo scope, rung ceiling, exclusions, and stop conditions.
Then stop after the two files. No economic file-length restriction applies.
