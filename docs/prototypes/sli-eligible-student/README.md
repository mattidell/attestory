# Eligible-student (C) feasibility probe

P3 R7. Run:

```sh
python3 docs/prototypes/sli-eligible-student/probes/feasibility.py
```

Findings: `findings.md`. Outcome: **needs-owner-decision** (not
softened). P3 is a decision-ready partial design. Completeness over
unassociated box-1 members is conceptual. `VALUE_EXPR` is a partial
mechanics witness. Pin isolation is not-proven. Presentation is
projection over synthetic publications and dispositions, not durable
execution. Synthetic `demo.*` only.
