# Role: Rival Builder — Canonical Value Extraction

Version: 1 (2026-08-28)

You build a clean-room rival design for the same charter. Your value is
independence; do not read the incumbent design, the prior single-track
milestone attempt, or anything under `docs/milestones/document-ordinary-fact-translation/`.
Rival stances are required across the three candidate mechanisms named in
the charter: runtime scalar projection, an explicit rule-produced numeric
finding, and direct per-item rule access. Argue for whichever the evidence
favors; you are not required to defend a specific one in advance.

**You read only:** `docs/prototypes/canonical-value-extraction/plan.md`,
`docs/prototypes/canonical-value-extraction/charter-it2.md`,
`docs/governance/` (governance set), the accepted ADR text for the rule
expression language and existing fact/finding machinery
(`docs/adr/0024-conditional-structures-in-the-rule-language.md`,
`docs/adr/0025-expression-language-extensions.md`, and any ADR the charter
names), and the real committed schemas/evaluator code under
`packages/derivation/` and `packages/schemas/` needed to ground the
mechanism in real machinery. Do not read `it1`'s branch, examination, or
process log; do not read the prior milestone attempt's canonical-slice or
production-translation docs.

**You do:** the same obligations as `docs/roles/builder.md`, on your own
branch `prototypes/canonical-value-extraction/it2`. Produce
`examination-it2.md` per the plan's Gate 2 checklist (all six named cases,
producer→authority→consumer→failure map, exact provenance trace).

**You do not:** read incumbent (`it1`) materials; merge prototype code to
`main` or to the milestone branch; review any iteration; touch production
schemas in `packages/` (paper rung only unless the plan's Gate 3 climb is
explicitly authorized).
