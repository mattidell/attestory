# Repair 2 Examination — F1–F4

Evidence rung: **Rung 1 static paper evidence only**.

Companion design: `repair2/design.md`. This examination measures the final
replacement layer; it does not treat prior self-assessment as evidence.

## Status

1. **F1: RESOLVED**
2. **F2: RESOLVED**
3. **F3: RESOLVED**
4. **F4: RESOLVED**
5. **T-F1: RESOLVED**
6. **T-F2: RESOLVED**
7. **REGRESSION BOUNDARY: INTACT**

## F1 — Four-component eligibility

**Resolved.**

- `design.md` §1 explicitly supersedes every affected retained
  three-component claim, topology list, semantics-map row, producer-map entry,
  `+3` cost, production count, E shorthand, eligible case, and evidence-index
  reference.
- §§2–3 leave one live definition of E over C1–C4, with C4 categorical
  `{yes,no}`, no default, presence-before-value, independently correctable,
  and distinct from box-2a closure.
- Case R2-E (§4) gives the exact member, horizon/closure, four current
  component finding identities, checked conclusion, line7a/7b, line9,
  taxable-income, and line16 path with values/dispositions and affected pins.
- Cases R2-M and R2-N (§5) distinguish
  `blocked(DEPENDENCY_ABSENT)`, `guard_inapplicable`, and non-publication.
  R2-Q2/R2-Q3 (§7.2) separately prove closure-backed numeric zero.

No box-2b/2c/2d family is proposed.

## F2 — Recoverable correction and supersession

**Resolved.**

`design.md` §6 names C4 findings v1/v2/v3, their current/displaced state, the
checked-conclusion versions, every line7a/7b/line9/taxable-income/line16
finding or disposition, and the pin aliases at L0, L1, and L2. The forward
transition adds v2=`"no"` and displaces the eligible chain. The reverse adds
v3=`"yes"` and new derived findings; it neither revives v1 nor overwrites v2.

## F3 — Contradictory Case 10 retired

**Resolved.**

`design.md` §1 retires it2 Case 10, its P3 unresolved question, production
flag, and evidence-index reference. Case R2-Q1 (§7.2) is the sole live
Q=`0`, positive-line7a outcome: QDCG is selected, worksheet line 3 is pinned
to line7a=`1500.00`, and that amount enters the preferential ladder. No
retained sentence prescribes ordinary-only tax for this state.

## F4 — Exact QDCG structure and cases

**Resolved.**

- `design.md` §7.1 partitions blocked, guard-inapplicable, and numeric line-7a
  outcomes before numeric selection. The structure is independent of
  incidental `all`/`any` operand ordering and never converts a disposition to
  zero.
- §7.2 supplies exact rows for Q=`0`/line7a=`1500`, Q=`50`/closure-backed
  line7a=`0`, and Q=`0`/closure-backed line7a=`0`, including selection,
  worksheet inputs, line9, taxable-income publication, line16 disposition,
  and pins.
- The positive-line7a and qualified-positive rows select QDCG; ordinary tax is
  selected only when both numeric publications are zero.
- Numeric line16 amounts are intentionally absent because the selected paper
  lacks the retained deduction and complete threshold inputs. The exact named
  publication/disposition and pin sets satisfy the charter without inventing a
  tax value.

## T-F1 / T-F2 and regression boundary

| Item | Status | Evidence |
| --- | --- | --- |
| T-F1 — complete Exception-1 authority | **RESOLVED** | `design.md` §§1–6 |
| T-F2 — correct QDCG selection and line-3 binding | **RESOLVED** | `design.md` §§1, 7 |
| successor/historical exclusivity | **INTACT** | `design.md` §8.1 |
| non-null presence signal and contradiction interlock | **INTACT** | `design.md` §8.2 |
| closure-backed zero | **INTACT** | `design.md` §§7.2, 8.3 |
| no raw downstream box-2a reads | **INTACT** | `design.md` §8.4 |
| honest non-publication | **INTACT** | `design.md` §§5, 8.5 |

## Superseded contradictions

The following no longer control the composite:

1. all it2 "three components," "all three," three-ref/pin, and `+3` claims;
2. the it2 component map, producer map, triple shorthand, and P1/P3
   evidence-index uses of three-component cases;
3. it2 Cases 1–6 only to the extent they claim three-component authority or
   affected downstream pins;
4. it2 Case 10, its ordinary-only outcome, unresolved question, production
   flag, and examination claims;
5. Repair 1's blanket retention sentence;
6. Repair 1 Cases 11–16 and their incomplete pin/lifecycle/QDCG evidence; and
7. Repair 1 examination's unsupported `RESOLVED` / `PASSING` statuses.

The exact locus-to-replacement mapping is `design.md` §1.

## Rung judgment and stop report

F1–F4 are resolved at Rung 1. Mechanical evaluator or package proof remains
outside this repair and is not needed to make the paper state partition,
authority, lifecycle, supersession, and cases recoverable.

No production, schema/content, fixture, test, probe, governance, Schedule D,
excluded-box source-family, real-data, topology, or fourth-proposition work
was performed.
