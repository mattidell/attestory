# Round 4 Foreman Triage

Date: 2026-07-12. Final Gate 5 classification for repair4.

| Finding | Classification | Disposition |
|---|---|---|
| All three reviewers reproduce the single supported `compute` entry and prior current-true, no-caller-carrier, ambiguity, and exact-pin cases. | converged evidence | SC-P1 shape A is sufficient for evaluation analysis. |
| Production must remove legacy `RunContext.closed_sets`, route every run through the selected entry, bind the adopted mapping, add real pins, and test persisted displacement. | `production-condition` | Milestone Tracks 2–3; not another prototype round. |
| Repair4 omitted the chartered computed Layer-1 zero regression; expressiveness incorrectly reported full coverage. Round 2 independently measured `5 + -5 → 0` without a closure pin. | `non-blocking defect` + `production-condition` | Preserve round-2 evidence; require the regression in production. Do not claim repair4 itself covered it. |
| Governance/expressiveness label repair4 as evidence level 4 although it is a throwaway evaluator refinement. | `non-blocking review defect` | Correct in analysis; level 4 persisted integration was never run. |
| Governance says underscore/private helpers prevent execution; Python privacy is conventional, not enforcement. | `non-blocking review defect` | Adopt only a supported-dispatch contract, exactly as repair4's examination limits it. |
| Adversary says future boxes require no mapping rewrite without measuring the claim. | `non-blocking review defect` | Exclude from evidence and ADR consequences. |
| SC-P2 identity and SC-P3 family semantics were not exercised in round 4. | `deferred-breadth` | Use earlier proposition-specific evidence only; no monolithic round-4 inference. |

The new Medium-tier model was mechanically disciplined and found the correct
central result, but its measurement precision was weaker than prior rounds.
These defects are preserved for process evaluation rather than repaired by
wording the reviews after the fact.
