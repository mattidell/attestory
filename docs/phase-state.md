# Phase State

This file is the re-entry point. Alongside the phase pointer below, it carries a **product briefing** in ordinary language, updated at milestone boundaries, answering four questions: what the product does now, what shims are in place, what the next milestone makes it do, and the nature of the pending schema/contract change.

## Product briefing (as of 2026-07-18, Foundation complete, Track 4c merged)

**What it does now.** Everything the source-complete W-2 and interest slices did, now flowing through Form 1040 lines **1a, 2b, 9, 11, 12, 15, and 16**. Taxable interest (line 2b) is an OID-inclusive *declared coextensive composition*: it publishes a value only when every constituent source family (1099-INT box 1, box 3, taxable OID, residual non-form) is closed, and blocks honestly otherwise — never a bare sum. Standard deduction and tax are declared rule artifacts: filing status is a first-class categorical domain, demographic inputs carry adopted defaults, and an asserted itemized amount overrides the calculated deduction. Each package closes over its full member surface (facts, rules, families, mappings, form-fields, citations, parameters, composition obligation); after adoption only the *resolved member graph* is executable and renderable (co-located content is inert), and both the package instance and every resolved member citizen are byte-verified before a run. A run records published and non-published dispositions in a durable ledger, and the explanation surface walks non-publication states without inventing a result. Citations are first-class versioned citizens with discriminated authority families, adopted through package membership.

**Shims in place.** Synthetic/demo-only; free supersession policy; E8.1 UI coverage deferred. Track 4c repaired the coordinator's missing live-path inputs with immutable successor content: core-v3 adopts an asserted rounding convention and a horizon-keyed W-2 closure, while the coordinator golden now starts from an authoritative fact log. It is merged; the owner-held real run and its workspace remain outside Git permanently. Citation *display* formatting is a deferred rendering contract. Named contract deferrals (not silent): ADR-0026's further positive interest sources (K-1, market discount) and the subtractive-adjustment mechanism (nominee/accrued/premium).

**What the active milestone makes it do.** The **First Real Return Slice** now has its three gate contracts ratified and crosses the synthetic boundary in Tracks 1–5: the owner's real W-2 / 1099-INT facts enter a live out-of-repo workspace through a contribution boundary, resolve through a production package resolver, and produce the owner's actual lines 1a/2b/9/11/12/15/16 with full explanations — while the repository provably carries zero personal data.

**Nature of the implementation boundary.** D1 real-data residency (ADR-0031), D2 contribution (ADR-0032), and D3 production package resolution (ADR-0033) are ratified. Tracks 4/4b/4c are all merged; the owner's next action is the quarantined real run and its non-descriptive attestation, after which Track 5 charters.

## Pointers

Active phase: **Real Return** — `docs/phases/real-return/` (Foundation completed 2026-07-15; its record: `docs/phases/foundation/foundation-roadmap.md`).

Canonical phase state lives in the phase roadmap: `docs/phases/real-return/real-return-roadmap.md`. Milestone selection in this phase is frontier-driven from `docs/phases/real-return/maturity-matrix.md`.

Active milestone: **First Real Return Slice** — **Tracks 0–4c are merged (Track 4c: PR #18, merge `2a2440e`); next: the owner's quarantined real run and non-descriptive attestation, then Track 5** — `docs/phases/real-return/milestones/first-real-return-slice.md`.

**Track 0 progress.** GitHub PR model live (ADR-0030): `main` is the continuous ratified record; merge-commit-only + `main` branch protection configured; agents push unit branches and open PRs, the owner merges. **D1 (real-data residency) ratified** as **ADR-0031** (owner, 2026-07-16, PR #3, merge `ce86525`): out-of-repo residency by rule with a capability wall, a total fail-closed classifier, independent commit + push envelope gates (ADR-0030 §C.8), no committed locator, independent-construction synthetic fixtures, and description-level sensitivity inheritance. D1's confirmation pass caught and corrected an over-firing foreman-authored locator screen (ADR-0013 guard). Milestone plan Verification corrected by version (Ontology §8 governs: disposition detail quarantined; only a non-descriptive attestation crosses). D1's production conditions are owed to milestone Tracks 1/3.

**D2 (contribution boundary) ratified** as **ADR-0032** (owner, 2026-07-16, PR #5, merge `c9dd3c4`): contribution as a first-class product event distinct from a run; provenance-bearing `finding.v2` retaining the Article 1 `evidence_ids` channel (rival basis) with an admission-time contribution↔evidence consistency check; the runs-consume-facts invariant settled on the request and declaration/edge sides, with a **MUST** production condition (marshal-only `RunContext` constructor + entrypoint-unreachability kill-test) making it structural at the runtime input; any-order scoped to independent facts; correction by supersession without horizon advance; D1 interlock consumed under ADR-0031's fail-closed default. A Gate-4 cap incident during D2 was resolved: the copy-pasted 900-line cap was miscalibrated (every real topic exceeded it), so the foreman role template's cap calibration was recalibrated (per-design ≤300, total ≤1,800 through committee, running total tracked each round). D2's production conditions are owed to milestone Track 2.

**D3 (production package resolver) ratified** as **ADR-0033** (owner, 2026-07-17): a current user adoption pins a verified release, which verifies registry bytes before package/member pins resolve into a strict `validation.ok == True` exclusive graph. Its ledger keeps D1/D2 as consumed owning-track conditions and names RG-1's eight core-package issues as a MUST repair — never an allowlist. D3 merged to `main` (PR #7). **Track 1 (boundary, contribution, and resolver schema citizens) complete and merged** (PR #8, merge `8d93ff1`): the D1/D2/D3 schema/contract citizens — classification, reserved-illustration-domain, fixture-provenance-manifest, finding.v2 (retained `evidence_ids` + optional `contribution_id`), closed run-request.v1, contribution/act-contribution/contribution-record, act-assertion.v2/act-member-transition.v2 successor carriers, and the D3 `release-registry.v1` + `act-package-adoption.v1` (Track 1 scope amended 2026-07-17, PR #9). Two author-independent pre-merge reviews cleared it. **Track 2 (contribution machinery) complete and merged** (PR #11, merge `aa5db67`): ADR-0032 runtime behavior over the Track-1 schemas, including the contribution applicator, contribution-record process account, record-state marshalling at `live_run`, static guard, and synthetic any-order/correction goldens. **Track 3 build landed** (`66a3497`) on `track/frrs-t3-resolver-bootstrap`: it implements ADR-0033 behavior over those schema citizens — sole-current-user adoption selection, verified release → registry → package/member resolution, hard `validation.ok == True` refusal gate, synthetic residency bootstrap with active D1 envelope guards, and the F1 evaluator fence. Its focused suite (30 tests), full suite (403 tests), mypy, and governance lint pass. **➡️ Next: an author-independent pre-merge review may be dispatched only with the owner's immediate, explicit approval; no review seat has been dispatched.** RG-1 repair, W-2 closure mapping, live-run integration, and the first real run remain Track 4. Interim: the GitHub remote stays **private** (standalone owner decision to change).

**Track 3 review disposition.** PR #13 was merged to `main` at `530e387`. Its owner-authorized review found F1: an identity-matching, schema-invalid co-located release could leak `SchemaValidationError`, violating typed fail-closed resolution. The owner-authorized narrow repair (`0001e74`) merged in PR #15 (`8c7af6d`), with a fresh merge-ready delta review at `docs/reviews/2026-07-18-frrs-t3-f1-release-inventory-delta-review.md`: the invalid candidate is inert beside the honest pinned release, and an invalid-only surface returns `RELEASE_ABSENT_OR_MISMATCH`. F2/F4/F6 and F3/F7 plus the PC(T4) carry are owned by the now-chartered Track 4 implementation (`docs/reviews/charter-2026-07-18-frrs-t4-w2-closure-live-integration.md`).

**Track 4 disposition.** The grouped implementation (`c39a79b`) was repaired after its first review found a forgeable envelope guard, late path refusal, and a mislabelled W-2 quantity. Repair `a08f37b` installs opaque guard authority, reserves outputs before run records, and publishes the distinct wages quantity with regenerated pins. Its fresh delta review is **merge-ready** (`docs/reviews/2026-07-18-frrs-t4-f1-f3-delta-review.md`): 43 focused tests, 416 full tests, mypy, governance lint, regeneration, safety scan, and all independent guard/path/quantity counter-probes pass. The owner merged Track 4 as PR #16 (`d8a2728`, 2026-07-18).

**Track 4b (repo-side envelope hooks).** The foreman's owner-directed post-merge review of Tracks 1–4 (2026-07-18) found ADR-0031's "installed, integrity-checked hooks" production condition discharged only as an in-process model (a real `git commit --no-verify` never executes `LiveWorkspace` gates), and the Track 1 D3 re-review record never committed. Owner-directed Track 4b (`docs/reviews/charter-2026-07-18-frrs-t4b-envelope-hooks.md`, branch `track/frrs-t4b-envelope-hooks`) commits that record and installs the repo-side gate: committed scanner + installer render byte-exact pre-commit/pre-push shims, and the suite byte-verifies the installation in every clone (fails with the remedy command when absent/tampered). Guarded transport (credential confinement) is an explicitly named deferral for the Track 5 ledger. Focused 12 / full 428 tests, mypy, governance lint green; the gate is live in the owner's clone and vetted this branch's own commits/push. The owner merged Track 4b as PR #17 (`f977b4d`, 2026-07-18).

**Track 4c live-path repair (2026-07-18, reviewed merge-ready).** The foreman's synthetic dry run found F1 record-pin drift, F2 missing `rounding.convention` vocabulary, and F3 an unkeyable W-2 closure. `57cdb64` repairs F1; `c5e3544` publishes the v3 core/W-2 successor content, adds the source-mapping successor loader behavior, and executes the missing coordinator-from-facts golden. The independent review at `7c490e7` found no blockers or scope defects, verified the full synthetic path and 433-test battery, and recorded one non-blocking wording correction: F1 aligns the record pin with the rule pin by requiring `origin` on input pins, so it is not purely additive for hypothetical origin-less legacy input pins. Full record: `docs/reviews/2026-07-18-frrs-t4-live-path-findings.md`; repair charter: `charter-2026-07-18-frrs-t4c-live-path-repair.md`; review: `2026-07-18-frrs-t4c-live-path-repair-review.md`. The owner merged Track 4c as PR #18 (`2a2440e`, 2026-07-18). The owner-held scaffold helpers (`tools/scaffold_live_acts.py`, `workspace-seed/`) are intentionally untracked per the charter. **➡️ Next: the owner performs the quarantined real run in `L` and records the exact three-fact attestation (ran the slice; dispositions observed in quarantine; no artifact crossed the boundary). Track 5 then charters.**

Durable history of the Foundation phase (milestones, remediations, corrections) lives in `docs/phases/foundation/foundation-roadmap.md`, the milestone retrospectives, and git history — no longer restated here.
