# First Real Return Slice — Track 4 Repair 1 Delta Review

Reviewer: owner-authorized, author-independent delta reviewer. Date:
2026-07-18. Branch: `track/frrs-t4-w2-closure-live-integration`. Reviewed
repair delta: `cad81d6` → `a08f37b`. Charter:
`docs/reviews/charter-2026-07-18-frrs-t4-f1-f3-delta-review.md`.
Original findings reviewed: F1–F3 in
`docs/reviews/2026-07-18-frrs-t4-w2-live-integration-premerge-review.md`.
This review changes no implementation, fixture, contract, ADR, schema, or
merge state.

## Verdict

**Merge-ready. No blocking, production-condition, or non-blocking finding in
the reviewed repair delta.** The repair closes all three named blockers without
changing published v1 bytes or expanding the Track-4 boundary.

## Measurements

### F1 — installed envelope authority and crossings

**Passed.** `InstalledEnvelopeGuards(Path(...), "fabricated", object())`
raises `TypeError`; a bare `object()` supplied to `guarded_commit` raises
`GuardIntegrityError`. After installation, independently exercised commit and
push entrypoints each reject a declared envelope whose first artifact is public
and whose later artifact has personal description lineage. Thus the whole
declared envelope is classified before either crossing.

The installed push gate was then overwritten and was refused with
`GuardIntegrityError`. `guarded_commit(..., no_verify=True)` and
`guarded_push(..., raw_transport=True)` independently refused. The repair no
longer treats a deterministic public path/hash or a public constructor as
authority: the accepted guard is minted only by the installed workspace and is
bound to its in-process installation authority and verified local gate pair.

### F2 — output reservation before run records

**Passed.** A valid synthetic coordinate run writes its declared output below
`L` and exactly its paired `started` and `completed` records. The independent
`output_name="../../escape.json"` probe raises `ResidencyViolation` before
either `L/records`, `L/outputs`, or the escaping target exists. The new
reservation is performed before the record stream is opened, so the refusal does
not manufacture a completed run account.

### F3 — distinct W-2 wages quantity and generated chain

**Passed.** `tax.us.2025.w2.box1-wages@v2` now pins
`tax.us.2025.quantity.wages@v1`, which differs from the 1099-INT box-1
taxable-interest quantity. The wages vocabulary citizen is included in the v2
package and published registry. Regenerating Track-4 content and Track-3
release/adoption fixture bytes left the committed content, registry, release,
and adoption files unchanged. The focused semantic golden also passes.

## Verification

- Independent F1/F2/F3 counter-probe script: all eleven checks passed
  (fabricated, missing, tampered, `--no-verify`, raw transport, complete commit
  and push envelopes, valid output, escaping output, and semantic quantity
  distinction).
- `python3 -m unittest tests.test_frrs_t3_resolver_bootstrap tests.test_frrs_t4_w2_live_integration -v` → 43 passed.
- `python3 -m unittest` → 416 discovered tests passed.
- `python3 -m mypy packages tools tests` → clean.
- `python3 tools/governance_lint.py` → conformant.
- `python3 tools/generate_frrs_t4_content.py` and
  `python3 tools/generate_frrs_t3_fixtures.py`, followed by a content/fixture
  diff check → no changed generated bytes.
- `git diff --check cad81d6 a08f37b` → clean.
- Full repair-delta scan for absolute local paths, personal-data directories,
  SSN/social-security terms, real-tax-document terms, and taxpayer markers →
  no matches. Committed fixtures remain synthetic.

## Scope and hand-off

The only pre-existing worktree item left untouched is the unrelated untracked
`docs/reviews/2026-07-17-frrs-t1-d3-schemas-rereview.md`. The repository's
`.venv/bin/python3` launcher is stale here and lacks `jsonschema`; the available
project `python3` (3.14, with the declared dependencies) completed the checks
above. That environment condition is not a defect in the reviewed delta.
