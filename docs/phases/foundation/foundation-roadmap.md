# Foundation Phase — Roadmap

Audience: Product (roadmap); Shared (status)

## Roadmap

1. **Governance Installation** — The governance set becomes the project's operating authority: versioned artifacts under `docs/governance/`, the two ratified closure-check fixes applied, the set stamped v0.1, planning scaffolding created, and a governance lint seeded as the first conformance tooling. This milestone comes first because every later contract cites the governance set, and agents cannot rely on documents that are still conversation-shaped drafts with unapplied defects.

2. **Workspace Kernel** — The workspace exists as code: an append-only act log, facts and findings with declared identity, supersession with derived currency, and schema-versioned citizens. This is the narrow waist every Constitution article cites; nothing downstream is buildable safely before it. Delivers the State-article conformance detections (E5.x, E6.1, E7.x, E1.1).

3. **Rule Language Design** *(added 2026-07-10)* — The encoding all tax meaning will live in, designed against real tax content through the prototype-driven decision process and ratified by ADR. Sequenced before machinery because the language is the project's most consequential contract, and building a runner around a placeholder was the failure that killed ADR-0004: you cannot discover what rules need by executing fake ones.

4. **Derivation Machinery** — Rule artifacts, the saturation runner, pin-complete derived findings, run records, and the adoption gate. Delivers the Computation-article detections (E4.1, E11.x, E12.1, E13.x, E14.x), including the portability reference runner that keeps the engine thin. Re-planned after the rule-language ADR is ratified; the archived first plan (`docs/archive/2026-07-10-derivation-machinery-plan/`) is the starting material.

5. **First Tax Slice** — A deliberately narrow W-2 box-1 to Form 1040 line-1a vertical slice: ratified fact identity, first-class form-field content, real rule/package execution, published-value explanations, correction/displacement, and explicit re-derivation over synthetic fixtures. Narrowed after Track 0 so implementation depends only on accepted ADR-0011/0012 contracts.

6. **Source Completeness And Interest Slice** — Resolve 1099-INT source-instance identity, adopted source-family mapping, closure-backed empty-source publication, and record-derived coverage before adding taxable interest. This is separate because Track 0 proved those are authority contracts, not ordinary content breadth.

7. **Core Tax Conditions And Presentation Integration** — Add downstream Form 1040 lines only after standard-deduction/tax-method condition structure, complete adopted-content manifests, citation authority, and non-publication explanations receive appropriately scoped decisions.

## Status

**Phase COMPLETE (2026-07-15).** All seven roadmap items closed; the remediated Core Tax Conditions milestone merged to `main` (`3283033`). The successor phase is **Real Return** (`docs/phases/real-return/`), which selects milestones from a maturity-matrix frontier rather than a pre-written ladder.

- Governance Installation — **complete** (2026-07-09, merge `6e4eefa`; retrospective: `docs/milestone-retrospectives/2026-07-09-governance-installation.md`). Impacts: `docs/governance/`, `docs/adr/`, `docs/milestone-retrospectives/`, root meta documents, `tools/`, `tests/`.
- Workspace Kernel — **complete** (2026-07-10, merge `c8799ce`; retrospective: `docs/milestone-retrospectives/2026-07-10-workspace-kernel.md`). Tier 2 decisions ratified 2026-07-10 (ADR-0002, ADR-0003): persistence (append-only JSONL act log), schema technology and identity (JSON Schema 2020-12, opaque IDs), synthetic kernel vocabulary, minimal adoption act. Impacts: `packages/kernel/`, `packages/schemas/kernel/`, `packages/sample_data/kernel/`, `tests/conformance/`, `docs/adr/`, `README.md`.
- Kernel Reconciliation patch — **complete** (2026-07-10, merge of `patch-kernel-reconciliation`): entity supersession act, record-only displacement, elective/basis coherence, consulted supersession policy. Closes the findings of `docs/archive/2026-08-02-milestone-artifacts/reviews/2026-07-10-workspace-kernel-tracks-4-7.md`.
- Rule Language Design — **complete** (2026-07-10; retrospective: `docs/milestone-retrospectives/2026-07-10-rule-language-design.md`). ADRs 0006/0007/0008 ratified; evidence at `docs/archive/2026-08-02-milestone-artifacts/prototypes/rule-language/evaluation-analysis.md`; iterations preserved as tags `exhibits/rule-language/it0`–`it2`. First run of the prototype-driven decision process, completed with two iterations of a three-iteration cap.
- Derivation Machinery — **complete** (2026-07-11, merge `e1608bf`; retrospective: `docs/milestone-retrospectives/2026-07-11-derivation-machinery.md`). All eight §5 ratification conditions discharged with cited tests (§5.6 deferred to First Tax Slice). Tier 3 ADR-0009 (derived-finding shape) ratified mid-build. Impacts: `packages/schemas/derivation/`, `packages/canon/derivation/`, `packages/derivation/`, `packages/sample_data/derivation/`, `tests/derivation/`, `docs/adr/0009`. Follow-up: publication-act envelope persistence into the kernel act log (combined-registry decision) not yet wired.
- Derivation Cascade Reconciliation patch — **complete** (2026-07-11, merge `18ce073`; closes the findings of `docs/archive/2026-08-02-milestone-artifacts/reviews/2026-07-11-derivation-cascade-reconciliation.md` under ADR-0010). Combined schema registry, act-log admission of derived-publication acts (kernel scopes to its own kinds), and a compose-over derivation-currency layer; supersession cascade golden proves W-2 → line1a → line9 displacement. Precedent: `patch-kernel-reconciliation`.
- First Tax Slice — **complete** (2026-07-11, merge `c548766`; retrospective: `docs/milestone-retrospectives/2026-07-11-first-tax-slice.md`). Track 0 concluded with ADR-0011/0012 and a narrow evaluation analysis. Four implementation tracks landed W-2 box 1 -> line 1a as a real rule/package over the existing runner, form-field content with all five ADR-0012 dispositions, slip-keyed identity with a two-slip and identity-collision negative, present zero, same-fact correction with derived-displacement cascade, and explicit re-derivation. Impacts: `packages/schemas/tax/`, `packages/content/tax/2025/`, `packages/tax/`, `packages/sample_data/tax/`, `tests/tax/`. Broader closure, 1099-INT, coverage, downstream tax conditions, citation authority, and non-publication explanations are deferred to roadmap items 6-7.
- Source Completeness And Interest Slice — **complete** (2026-07-12, branch `milestone/source-completeness`, merge `382a7af`; retrospective: `docs/milestone-retrospectives/2026-07-12-source-completeness-and-interest-slice.md`). Track 0's three decision topics ratified ADR-0014 through ADR-0017; five implementation tracks landed the contract schemas, atomic horizon projection through existing individuation/derivation currency, adopted-mapping single dispatch (RunContext.closed_sets removed), 1099-INT box-1 statement-instance content with the B1 subtotal (no line-2b binding, ADR-0016), and the closure-backed zero / open-block lifecycle with a record-derived exact-claim coverage read model. Impacts: `packages/schemas/kernel/`, `packages/schemas/derivation/`, `packages/kernel/horizons.py`, `packages/derivation/`, `packages/content/tax/2025/`, `packages/tax/`, `packages/sample_data/`, `tests/source_completeness/`. Line 2b, Schedule B, and other 1099-INT boxes remain roadmap item 7; the W-2 family closure mapping and workspace-service membership routing are recorded follow-ups.
- Source Completeness Reconciliation patch — **complete** (2026-07-13, merge `bf23517` of `patch-source-completeness-reconciliation`; closes SC-R1/SC-R2 of `docs/archive/2026-08-02-milestone-artifacts/reviews/2026-07-13-source-completeness-reconciliation.md` under ADR-0023, ratified at merge). Kernel-enforced admission boundary: plain assertions of predicate-matching member facts rejected (must route through member transitions); same-member corrections rejected on the transition path (no horizon advance, ADR-0017 d4). Registry-carried member predicates populated by the tax loader keep the kernel content-agnostic. Pre-merge review: `docs/archive/2026-08-02-milestone-artifacts/reviews/2026-07-13-source-completeness-patch-review.md`. Precedents: `patch-kernel-reconciliation`, Derivation Cascade Reconciliation.
- Core Tax Conditions And Presentation Integration — **planning** (proposed 2026-07-12; plan: `docs/archive/2026-08-02-milestone-artifacts/phases/foundation/milestones/core-tax-conditions-and-presentation-integration.md`). Settle taxable-interest composition, condition structure for standard-deduction/tax-method selection, adopted-content manifests, citation resolver, and non-publication explanations. Impacts: `packages/schemas/tax/`, `packages/content/tax/2025/`, `packages/tax/`, `packages/sample_data/`, `tests/tax/`.
- Core Tax Conditions And Presentation Integration — **in implementation** (plan: `docs/archive/2026-08-02-milestone-artifacts/phases/foundation/milestones/core-tax-conditions-and-presentation-integration.md`). Tracks 0 and 1 completed (ADRs 0018-0022 ratified, schemas published).
- Core Tax Conditions And Presentation Integration — **paused for governance remediation, remediation nearly complete** (plan: `docs/archive/2026-08-02-milestone-artifacts/phases/foundation/milestones/core-tax-conditions-and-presentation-integration.md`). Correction 2026-07-13: the prior "Tracks 0 and 1 completed, ADRs 0018-0022 ratified" claim was wrong — ADRs 0018/0021/0022 remain proposed, ADR-0019 is rejected (round-1R evidence), ADR-0020 is redrafted and proposed, and Track 1/2 implementation was removed from the milestone branch by owner-directed reset (`fc9a855`; pre-reset history at `archive/core-tax-conditions-pre-reset`). See `docs/phase-state.md` and `docs/foreman-handoff.md`.
- Core Tax Conditions And Presentation Integration — **complete** (2026-07-15; remediated re-close). Track 0 ratified ADRs 0020, 0024–0029 (0019 rejected); Tracks 1–6 landed lines 1a/2b/9/11/12/15/16, OID-inclusive declared interest composition, conditional standard-deduction/tax logic, manifest closure with exclusive projection + package/member byte verification, citation resolution, and the run-disposition ledger + non-publication walk. The first close was a premature autonomous merge (`2fbc3a7`) without owner go or pre-merge review; it was reopened, given a retrospective pre-merge review (PMR-1–7), remediated R1–R4R (independent `ready`), rebased onto `7a90f89`, and merged once to the reconciled `main` per ADR-0030. Process outcomes: ADR-0013 amendment (foreman-authored fixes default to confirmation) and ADR-0030 (branch-and-merge strategy) ratified. Retrospective: `docs/milestone-retrospectives/2026-07-15-core-tax-conditions-and-presentation-integration.md`.


### Implementation notes

- 2026-07-09: The recomposed Principles document was initially missing from the repository; the user recovered it as `docs/INTAKE_PRINCIPLES.md` during milestone planning. The full five-document set stamps v0.1 in this milestone.
- 2026-07-10: Workspace Kernel completed in a secondary worktree continuation branch because the original milestone branch was checked out elsewhere at Track 3. The final merge to `main` preserves one implementation commit per track.
- 2026-07-10: ADR-0004 rejected by the owner — its central design element (the rule language) was a placeholder. The rejection produced the prototype-driven decision conventions and the Rule Language Design milestone; the derivation plan was snapshotted and reset per the development posture.
