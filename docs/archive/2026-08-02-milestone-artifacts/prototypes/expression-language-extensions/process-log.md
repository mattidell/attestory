# Expression Language Extensions Prototype — Process Log

## 2026-07-13

- Topic split from conditional-selectors round 2R: the optional-input absence gap (confirmed impossibility under committed contracts) and categorical comparison (CS-G8R legibility concession) are one contract surface. Owner approved the topic and chartering in the same directive; plan drafted by the principal foreman under that authority (owner may amend before builder launch).
- `charter-it1.md` issued for a High-tier incumbent on the plan's five Gate 2 cases; Rung 2 (paper + scratch probes) authorized because the predecessor topic's failure mode was paper that did not execute. ADR-0024 delegates its interim decisions 5–6 to this topic; candidate ADR number 0025.
- Incumbent exhibit delivered (owner-launched external context): `it1/design.md` + `examination-it1.md`. Foreman conformance check PASSED (scope only, merits deferred to committee): two files only, examination 70 lines (≤120), all five Gate 2 cases present with two positives / two negatives each and Case 2's mandatory pin-and-edge-named displacement trace, Rung 2 boundary held (HEAD impossibility reproduced with throwaway probes; contract changes as versioned paper diffs; no evaluator/runner/schema/git writes; unresolved authority questions reported). Committed under foreman custody at `a9e4b9c`. Notable design elements for the committee to weigh (not conformance): new `optional-input-declaration.v1` citizen, `default` pin role, a proposed `default_superseded` displacement root class refining ADR-0010 decision 5, and a new `match` op for categorical eq/ne.
- `charter-it2.md` issued for the clean-room rival (High tier) — same two propositions and five cases, sealed from the it1 outputs to keep convergence evidence genuine per the ADR-0013 amendment. Awaiting owner launch.
- Clean-room rival exhibit delivered (owner-launched external context): `it2/design.md` + `examination-it2.md`. Foreman conformance check PASSED (scope only, merits deferred to committee): two files, examination 69 lines (≤120), all five Gate 2 cases with two positives / two negatives each, Case 2's mandatory initial-run-then-assertion trace naming every pin and edge, Rung 2 boundary held (paper schema/canon diffs, no evaluator/runner/schema/git writes, unimplemented primitives not claimed to execute at HEAD), clean-room attestation present. Committed under foreman custody at `b2b9022`. Divergences for the committee (not conformance): displacement mechanism — incumbent's `default_superseded` root class (refines ADR-0010 decision 5) vs rival's extension of the existing correction fold to derived-finding answers sharing a `fact_id`; categorical op — incumbent's `match` vs rival's `categorical_compare` + `category_literal`; declaration shape — incumbent's `optional-input-declaration.v1` citizen + `default` pin vs rival's package-level `input_bindings` + `resolved_input` branch on `derived-finding.v2` + `default` pin.
- ADR-0024 ratified by owner (`c428f2c`); its interim decisions 5–6 now formally delegated to this topic's outcome.
- Committee round chartered (Gate 4: two Medium reviewers, independent contexts) over **both** designs: `roles/reviewer-governance.md` (Article 7/11 edge + declaration conformance, ADR-0012 vocabulary, per-design displacement-mechanism judgment) and `roles/reviewer-adversary.md` (three refuted workarounds, displacement ordering, pin-origin integrity, categorical break attempts, Case-5 floor). Each reviewer gives a verdict per proposition per design and a carry-forward recommendation. Awaiting owner launch; on arrival expect `reviews/round-1-governance.md` (ELX-G*) and `reviews/round-1-adversary.md` (ELX-A*). Candidate ADR-0025.

## 2026-07-14

- Committee reviews delivered (owner-launched external contexts) and committed under foreman custody: `reviews/round-1-governance.md` (ELX-G1–G6, 110 lines) and `reviews/round-1-adversary.md` (ELX-A1–A8, 101 lines). Both passed conformance (per-proposition-per-design verdicts, independence exclusions attested, neither read the other). **Unanimous outcome:** both reviewers carry the **rival (it2)** design forward for both ELX-P1 and ELX-P2 and reject the incumbent's two distinctive mechanisms. The two designs converged (independent authorship) on six load-bearing principles; divergence was isolated to mechanism, and every decision-blocking finding resolved for the rival on governance *and* a concrete adversary counterexample (ELX-A1 multi-default collision, ELX-A3/A4 silent categorical false, ELX-A5 unspecified migration).
- Foreman triage folded into `evaluation-analysis.md` (economical — reviewers classified every finding and unanimously converged, no conflict to adjudicate). Four decision-blocking divergences D1–D4 (displacement mechanism, declaration structure, comparison contract, code→label migration) all for the rival; three production conditions PC1–PC3 (transitive pin origin, new `CATEGORICAL_DOMAIN_MISMATCH` disposition, unexecuted-at-HEAD). Gate 6 minimum converged subset met; no repair pass needed.
- **ADR-0025 drafted (proposed)** — adopts the rival mechanisms for both propositions with PC1–PC3; resolves ADR-0024's delegated interim decisions 5–6. Awaiting owner ratification.
- **ADR-0025 ratified by owner (2026-07-14).** Status proposed → accepted. Topic CLOSED. Both rule-language engine decisions (0024, 0025) are now ratified, so the milestone's Track 1 (conditional-structure schemas) and Track 3 (conditional structures) rebuilds are unblocked; ELX production conditions PC1–PC3 land in that implementation.
