# Examination — Attachment Ontology it2 (Clean-Room Rival)

Date: 2026-07-19. Builder: sealed clean-room rival. Evidence: Rung 1 paper
only. Design: `docs/archive/2026-08-02-milestone-artifacts/prototypes/attachment-ontology/it2/design.md`. Synthetic
`demo-*` throughout. No climb taken.

## Proposition verdicts

### D1-P1 — Attachment citizen — **settled-at-rung**

Three states as published walkable dispositions (`not_required` /
`required_complete` published findings; `required_incomplete` as ADR-0020
ledger blocked row — never silence). Requirement is declared rule content over
2b and 3b symbols with parameter threshold and instruction citation.
Incompleteness blocks only the attachment symbol; sibling lines do not depend
on it — isolation by dependency graph construction.

| Case | Result | Citation in design |
|---|---|---|
| 1a required walk (D=1600, I=900) | Pass — triggers ordinary dividends only; `gt`+citation | §Gate-2 case 1a; §D1-P1 requirement rule |
| 1b not-required publishes | Pass — same pin shape, triggers empty | case 1b |
| 1c exactly $1,500 | Pass — not over (`gt`); 1500.01 requires | case 1c |
| 3 kill-case block placement | Pass — A blocked; 2b/3b/3a still publish | case 3; §blocking by construction |
| 6 generalization | Pass — Schedule D stub uses only `attachment.v1` | case 6 |

Threshold/completeness express under committed `rule-artifact.v2` + evaluator
`choose`/`ref`/`compare` — climb not required.

### D1-P2 — Itemization + tie-out — **settled-at-rung**

Repeating rows via paper canon op `collect_members` (committed `collect` lacks
identity keys — declared extension, not a conditional gap). Declared `eq`
tie-out to line symbols; coextensive when same closed family/revision.
Divergence guard: fixed run revision, horizon-keyed closure (ADR-0017), package
slot check, pin displacement (Article 7).

| Case | Result | Citation |
|---|---|---|
| 2 complete with rows | Pass — four demo payers pinned; subtotals eq I/D | case 2; §D1-P2 |
| 4 tie-out + supersession | Pass — R2 co-moves rows and line; R1 run-scoped | case 4 |

### D1-P3 — Part III assertions — **settled-at-rung**

Three determinable fact types on the filing-status pattern, no optional
defaults, contributed via ADR-0032. Yes-branch names FinCEN 114 with
`produced: false`; missing answer or missing country → case-3 incompleteness.

| Case | Result | Citation |
|---|---|---|
| 2 Part III no/no | Pass — complete body | case 2 |
| 3 Part III absent | Pass — DEPENDENCY_ABSENT on A | case 3 |
| 5a yes + country | Pass — named FinCEN, not produced | case 5a |
| 5b yes, country missing | Pass — incomplete posture | case 5b |

## Case roll-up (all six)

| # | Mandatory? | Outcome |
|---|---|---|
| 1 hard trace both + boundary | yes | settled |
| 2 required complete | | settled |
| 3 required incomplete kill | yes | settled |
| 4 row/line tie-out | | settled |
| 5 Part III yes-branch | | settled |
| 6 Schedule D generalization | yes | settled |

## Unresolved / non-claims

- Runtime wiring of `collect_members` and `attachment.v1` is Track 1–2
  production, not this paper round.
- Exact FinCEN statutory citation text beyond the named obligation label is
  content authorship at implementation.
- No proposition left **unresolved** at Rung 1 for charter questions D1-P1–P3.

## Seal

No incumbent `it1/` or `examination-it1.md` material was sought or used.
