# Round 2 Foreman Triage

Date: 2026-07-12. Gate 5 classification over executable SC-P1 reviews.

| Finding | Classification | Routing |
|---|---|---|
| Current-literal-true admission, false/absent/displaced/ambiguous blocking, exact pins, and declared caller-union mutants pass for both shapes. | `non-blocking defect` cleared / converged evidence | SC-P1 enforcement mechanics are supported; retain tests for production translation. |
| `ClosureAuthority` and `ResolvedMembership` are publicly constructible; `Env` trusts fabricated and duplicate carriers, allowing zero publication and stale-first pins outside either resolver. | `decision-blocking` (SC-P1 construction boundary) | One bounded repair of shape A only: resolver-controlled construction or mandatory validation, one authority per family, fabricated/duplicate carriers rejected before evaluation. |
| Shape B treats a second adopted collecting rule for the same family as conflicting authority, causing an ordinary rule-addition outage. | `decision-blocking` for shape B selection | Reject shape B. Select shape A's independently adopted reusable mapping as the surviving SC-P1 contract candidate. No repair of shape B. |
| Production still exposes `RunContext.closed_sets`; real pins and persisted displacement remain unimplemented. | `production-condition` | Milestone Tracks 2–3; prototype code never enters production directly. |
| Computed-zero, wrong-scope, and cross-source fixtures were not all committed, though independent probes passed the first distinctions. | `production-condition` / `non-blocking defect` | Add production contract tests; do not reopen SC-P2/SC-P3 here. |
| Repair2 exceeded the original one-repair cap under delegated owner authority but the approved plan did not state the amendment. | `non-blocking process defect` | Amend Gate 4 explicitly: foreman may authorize one additional bounded pass at a time for the central question under the owner's 2026-07-12 delegation/preference. Preserve governance dissent and this correction. |

## Disposition

SC-P1 is not yet ready for analysis because the carrier trusted by the copied
calculation path can be fabricated. Continue with `repair3`, limited to the
shape-A construction/invariant boundary. The pass succeeds only if evaluation
accepts authority produced by validated shape-A resolution, rejects fabricated
or duplicate carriers, preserves exact pins, and retains every prior negative.
No further shape comparison, schema work, production integration, SC-P2, SC-P3,
or SC-D1 enters the repair.
