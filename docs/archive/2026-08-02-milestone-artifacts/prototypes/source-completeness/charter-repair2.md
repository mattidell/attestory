# Charter: Repair Pass 2 — SC-P1 End-to-End Sole-Writer Check

Date: 2026-07-12. Foreman-authorized under the owner's delegated progression
authority and bounded-iteration preference after repair1 reached the declared
production-adapter boundary.

- **Branch:** `prototypes/source-completeness/repair2`
- **Evidence level:** throwaway evaluator only. Copy the production two-layer
  `collect` behavior into the prototype; do not import, edit, or execute the
  production runner/evaluator.
- **Builder seat:** original it1 builder, deliberate continuity, High tier
  (high effort).
- **Question:** when the real two-layer empty-source check consumes repair1's
  resolved authority, is that resolver the sole possible writer of closed
  membership, with caller injection unrepresentable and the exact closure
  finding retained for explanation?
- **Scope:** SC-P1 only, both surviving mapping shapes. SC-P2, SC-P3, SC-D1,
  production packages, and production schemas remain untouched.

## Inputs

- `charter-repair1.md` and `exhibits/source-completeness/repair1`.
- Both paper exhibits, round-1 reviews, and `round-1-triage.md`.
- Current production `collect`, `RunContext`, derivation, and pin-building seams
  for faithful copying and comparison only.

## Deliverables

All work stays under `docs/archive/2026-08-02-milestone-artifacts/prototypes/source-completeness/repair2/`, plus
`examination-repair2.md` (≤ 200 lines):

1. A minimal throwaway copy of the real two-layer `collect` check wired only to
   repair1's resolved membership carrier for both authority shapes.
2. A construction path with no caller-supplied closed-set argument, union,
   setter, alternate environment constructor, or bare-family injection seam.
3. Tests proving:
   - present members publish their aggregate without consulting closure;
   - empty members publish zero only for one exact current literal-true closure;
   - false, absent, displaced, ambiguous, and truthy values block;
   - a bare caller family string cannot cause publication;
   - explanation pins contain the exact closure finding used by admission;
   - duplicate mappings/rules block rather than choose or overwrite authority.
4. At least one end-to-end presence-only or caller-union mutant killed by the
   false-closure or injection case for each shape.
5. `examination-repair2.md`: commands/results, correspondence to the real
   two-layer check, all negatives, and one final call on whether SC-P1 is
   settled or what exact executable uncertainty remains.

## Pre-declared checks

1. The copied calculation behavior preserves production layer-1/layer-2
   semantics; deviations are listed explicitly.
2. Resolved authority is the sole source of closed membership by construction.
3. Every repair1 negative remains blocked through publication, not merely at
   resolver output.
4. Successful empty-source publication pins the exact current-true finding.
5. Both mapping shapes receive the same cases and assertions.
6. No production import, mutation, schema, workspace, persistence, or coverage
   work occurs.

## Stop conditions

Stop after this executable path test. Do not translate prototype code into
production. Any residual uncertainty must name a production-only behavior that
the faithful copy cannot represent; adjacent content or identity questions are
deferred rather than absorbed.
