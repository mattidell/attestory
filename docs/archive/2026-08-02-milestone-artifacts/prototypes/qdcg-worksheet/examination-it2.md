# Examination — Clean-Room Rival D2, Iteration 2

Scope observed: sealed from incumbent materials; only the charter's required
entry chain was used.  This is Rung-1 paper evidence.  No authorized Rung-2
probe was required: committed evaluator source establishes the expression
construction, and committed derivation-currency source establishes the two-hop
input-pin displacement path.

| Proposition | Verdict | Evidence |
| --- | --- | --- |
| D2-P1 — categorical declared absence and presence semantics | **Settled at Rung 1** | `design.md` P1 and cases 3/5 use two `{yes,no}` contributed fact types and no-default, presence-based certificates. Certificates avoid treating `no` as absent and create explicit declaration → certificate → line-16 pins, as required by ADR-0036 and ADR-0010. |
| D2-P2 — ladder, reduction, and supersession | **Settled at Rung 1** | `design.md` P2 and cases 1/2 express split, all three rate portions, comparison, final minimum, and final round using committed operations. The algebra proves `Q=0` reduces to ordinary tax. The two-rule, adopted conditional selector supersedes v1 without requiring declarations for a no-qualified-dividend return. |
| D2-P3 — bidirectional contradiction | **Settled at Rung 1, with a named production contract** | `design.md` P3 and case 4 specify an adopted `admission-constraint.v1`, post-batch/precommit evaluation, atomic rejection, and all three order cases. It is a versioned schema/canon delta, not a policy check or a new standing edge. |

Mandatory-case disposition:

1. Positive full ladder: passed on synthetic `demo-single` parameters; `8,410 < 8,500`.
2. Reduction: algebra and selector both preserve the ordinary result at `Q=0`.
3. Missing declarations: certificate absence blocks line 16 with both contributable facts named.
4. Contradiction: declaration-first, statement-first, and same-batch candidates are all rejected before mutation.
5. Declared zero: `no/no` produces both certificates and publishes; later supersession displaces the full chain.
6. No reach-around: box 2a/signal is absent from all worksheet rule bindings and enters solely through admission rejection.

No unresolved decision-blocking issue remains on paper.  The proposed admission
constraint and certificate content are not claimed as implemented; Track 2/3
must provide the named schema, runtime, and coordinator-from-facts kill tests.
