{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "derivation/dividend-universe.v2",
  "title": "Successor declared dividend universe with composable box 2a",
  "description": "Successor Form 1099-DIV box universe (ADR-0050 decisions 2-3): composable boxes {1a, 1b, 2a}, each bound to exactly one source family, and residual recorded-non-composable boxes {3, 5, 7, 12} without box 2a. The capital-gain signal remains CAPITAL_GAIN_DISTRIBUTION_RECORDED and is sourced from a current successor box-2a family member with a non-null amount. dividend-universe.v1 remains immutable history.",
  "type": "object",
  "properties": {
    "schema": {
      "const": "dividend-universe.v2"
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(\\.[a-z0-9-]+)+$"
    },
    "version": {
      "type": "string",
      "pattern": "^v[0-9]+$"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "scope": {
      "type": "object",
      "properties": {
        "tax_year": {
          "type": "integer"
        },
        "jurisdiction": {
          "type": "string",
          "minLength": 1
        },
        "family": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "tax_year",
        "jurisdiction",
        "family"
      ],
      "additionalProperties": false
    },
    "composable_boxes": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "items": {
        "type": "object",
        "properties": {
          "box": {
            "enum": [
              "1a",
              "1b",
              "2a"
            ]
          },
          "family": {
            "$ref": "#/$defs/pin"
          }
        },
        "required": [
          "box",
          "family"
        ],
        "additionalProperties": false
      },
      "allOf": [
        {
          "contains": {
            "properties": {
              "box": {
                "const": "1a"
              }
            },
            "required": [
              "box"
            ]
          }
        },
        {
          "contains": {
            "properties": {
              "box": {
                "const": "1b"
              }
            },
            "required": [
              "box"
            ]
          }
        },
        {
          "contains": {
            "properties": {
              "box": {
                "const": "2a"
              }
            },
            "required": [
              "box"
            ]
          }
        }
      ]
    },
    "recorded_non_composable_boxes": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "uniqueItems": true,
      "items": {
        "enum": [
          "3",
          "5",
          "7",
          "12"
        ]
      }
    },
    "recorded_boxes_fact_type": {
      "$ref": "#/$defs/pin"
    },
    "capital_gain_signal": {
      "type": "object",
      "properties": {
        "box": {
          "const": "2a"
        },
        "signal": {
          "const": "CAPITAL_GAIN_DISTRIBUTION_RECORDED"
        },
        "source": {
          "const": "current_successor_family_member"
        }
      },
      "required": [
        "box",
        "signal",
        "source"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "schema",
    "id",
    "version",
    "title",
    "scope",
    "composable_boxes",
    "recorded_non_composable_boxes",
    "recorded_boxes_fact_type",
    "capital_gain_signal"
  ],
  "additionalProperties": false,
  "$defs": {
    "pin": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "version": {
          "type": "string",
          "pattern": "^v[0-9]+$"
        }
      },
      "required": [
        "id",
        "version"
      ],
      "additionalProperties": false
    }
  }
}
